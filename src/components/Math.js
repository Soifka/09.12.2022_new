export const sum = (a, b) => a + b;

export const sub = (a, b) => a - b;

export const multy = (a, b) => a * b;

// export default {sum, sub, multy}; --> это вариант вместо прописывания export перед каждой функцией

